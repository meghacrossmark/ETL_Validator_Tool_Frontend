import { CleanSuffixPipe } from './clean-suffix.pipe';

describe('CleanSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
