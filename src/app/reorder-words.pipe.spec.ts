import { ReorderWordsPipe } from './reorder-words.pipe';

describe('ReorderWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReorderWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
